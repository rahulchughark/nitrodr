# Nitro:

#27-02-2026:
ALTER TABLE `orders` CHANGE `is_approved` `is_approved` INT(11) NOT NULL DEFAULT '0' COMMENT '0 : not approved, \r\n1 : approved,\r\n2 : Reject';


#02-Mar-2026:
ALTER TABLE `orders` ADD `created_by_category` ENUM("Partner", "Internal") NULL DEFAULT NULL AFTER `is_opportunity`;

ALTER TABLE `orders` ADD `close_date` DATE NULL DEFAULT NULL AFTER `is_opportunity`;


#06-Mar-2026:
ALTER TABLE `orders` CHANGE `is_approved` `is_approved` INT(11) NOT NULL DEFAULT '0' COMMENT '0 : not approved, \r\n1 : approved,\r\n2 : Reject,\r\n3 : Onhold';


#10-03-2026:
ALTER TABLE `orders` ADD `lead_source_id` INT NULL DEFAULT NULL COMMENT 'Reference to lead_source.id' AFTER `industry_id`;
ALTER TABLE `orders` ADD `comment` LONGTEXT NULL DEFAULT NULL AFTER `created_by_category`;
ALTER TABLE `orders` ADD `product_interest` VARCHAR(254) NULL DEFAULT NULL COMMENT 'Product of Interest' AFTER `created_by_category`;



#30-03-2026:
ALTER TABLE `orders` ADD `description` INT NULL DEFAULT NULL AFTER `comment`;

#31-03-2026:
ALTER TABLE `orders` ADD `sub_product` VARCHAR(254) NULL AFTER `product`;



#09-04-2026:
ALTER TABLE `opportunity_attachments` ADD `parent_id` INT NULL DEFAULT NULL COMMENT 'Invoice upload reference linked to its respective PI' AFTER `id`;
ALTER TABLE `opportunity_attachments` CHANGE `parent_id` `parent_pi_id` INT(11) NULL DEFAULT NULL COMMENT 'Invoice upload reference linked to its respective PI';

ALTER TABLE tbl_mst_invoice
ADD COLUMN advance_amount DECIMAL(15,2) DEFAULT 0 AFTER tds;


#28-04-2026:
ALTER TABLE `orders` ADD `license_type` VARCHAR(254) NULL DEFAULT NULL AFTER `lead_source_id`, ADD `renewal_type` VARCHAR(254) NULL DEFAULT NULL AFTER `license_type`;